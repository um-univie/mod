API Reference
=============

The following header files specify the external API for the library, i.e., all
declarations directly in the ``mod`` namespace. The nested namespace ``mod::lib``
contains the actual implementation, and you probably do not want to use
anything from it directly.

.. include:: Toc.rst
